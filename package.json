{
  "name": "ai-education-platform",
  "version": "1.0.0",
  "description": "AI-powered education platform with gamification and real-time feedback",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "test": "npm test -w frontend && npm test -w backend",
    "test:properties": "npm run test:properties -w frontend && npm run test:properties -w backend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
